%% datasample
clc
asdf = 1:3;
n_samples = 2;
datasample(asdf,n_samples,2,'Replace',false)

%% idx
A = 1:10;
idx = find(A<7);
B = A(idx); 


%% dist
clc
a = [1 1;0 0];
b = [0 0;1 0]; 
diff = abs((a-b)); dist = dist()


